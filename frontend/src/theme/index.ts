import {createMuiTheme} from '@material-ui/core';

export const templateTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#9800ff'
        },
        secondary: {
            main: '#ff9900',
            contrastText: '#f5f5f5'
        }
    }
});