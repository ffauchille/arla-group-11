import { Button } from "@material-ui/core";
import React from "react";

export const Logout: React.FC = () => {
  return (
    <Button
      variant="outlined"
      color="primary"
      onClick={() => {
        {}
      }}
    >
      Logout
    </Button>
  );
};
