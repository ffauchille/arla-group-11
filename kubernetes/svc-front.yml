apiVersion: v1
kind: Service
metadata:
  name: "arlaide-front"
  namespace: "groupe-11"
  labels:
    app.kubernetes.io/component: "front"
    app.kubernetes.io/instance: "arlaide-front"
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/component: "front"
    app.kubernetes.io/instance: "arlaide-front"
  type: ClusterIP
