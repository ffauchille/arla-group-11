apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: arlaide-api
  namespace: "groupe-11"
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: arlaide-api
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50